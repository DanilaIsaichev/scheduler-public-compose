# scheduler-public-compose

BACKEND_URL - переменная, в которую записывается адрес сервера с портом, на котором развёрнут scheduler-private-compose.

HOURS - время (час) в которое обновляются файлы календарей.
